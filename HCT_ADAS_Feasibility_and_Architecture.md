# HCT ADAS V1 精简版可行性与技术边界

> 更新于 2026-09-19。本文保留 V1 原理和可行性边界，实施状态与验收以 [V1 精简版方案与实施计划](HCT_ADAS_Final_Solution_and_Implementation_Plan.md) 为准；V2 双模型和 ByteTrack 方案已放弃。
>
> 原报告中的 JNI/NCNN/YOLO 方案是早期备选；本项目已采用 Java + LiteRT。原先“经实测 12–18 ms”“40–60 MB”“过滤 85% 主动变道误报”“CPU 消耗归零”“1 周完成”等表述没有本项目验证依据，本次撤去。

> **当前落地边界（2026-09-19）：** 本版按后装精简产品交付，`LDW_ENABLED=false`，暂时关闭实时车道检测、车道绘制和行车自动车道标定；保留前车检测、跟踪、单目距离、FCW/HMW/LVSA。当前设置入口为无背景 HUD 按钮，设置页只保留车型、镜头 HFOV 和 ADAS 校准；校准页用水平线与竖直线做小范围安装对齐，不显示中央安装提示。标定只按当前画面分辨率保存，不绑定摄像头序列号。文中关于车道算法、车道标定和 LDW 的内容属于历史设计或后续可选能力，不代表当前生产 UI 链路。

## 项目级复核结论（2026-09-18）

当前代码已经形成“USB/UVC → NV21 快照 → 有界队列 → LiteRT 检测 → 前车跟踪 → 单目测距/运动量 → FCW/HMW/LVSA 决策 → UI 与固定报警音”的可运行前台原型链路；车道类仍在仓库中但当前产品开关关闭。用户已确认编译正常，但本轮没有重新编译、执行 Gradle 测试或做设备/实车验证，因此不能把当前状态称为量产软件。

复核后必须按以下边界理解实现：

- 当前 V1 由紧凑设置页和双线校准写入 `DISTANCE_READY` 后直接开放距离能力；历史 `CALIBRATED` 只表示旧车道俯仰学习达到收敛。两者都不是完整的相机内参、畸变、主点、yaw 或横向安装偏差标定。
- HMW 已加入 THW 计算，但当前仍保留固定距离兜底：速度 ≥15 km/h 时普通提醒为 `THW≤1.2 s 或距离≤8 m`，更低速按距离≤8 m，停车时普通视觉提示仍可显示；极近规则为 `THW≤0.6 s 或距离≤4 m`，停车时抑制极近音频。该规则仍需产品冻结和实车标定。
- FCW 由连续 3 个分析帧和 `FCW_CONFIRM_MILLIS=200` 的时间窗共同确认，触发延迟仍会随推理耗时、丢帧和设备性能变化。
- LDW 仍保留固定 ROI 的亮边缘原型，但当前 `LDW_ENABLED=false`，生产帧不会执行车道检测、车道绘制或 LDW 决策；车道章节仅作为后续恢复设计。
- 报警音实际走 `SoundPool` 的 `USAGE_MEDIA` 和 `ToneGenerator(STREAM_MUSIC)` 软件路径；高优先级事件会停止已播放的低优先级 SoundPool 流和 ToneGenerator，但没有独立 ADAS 音频通道、AudioFocus、硬件蜂鸣器或总开关，正式构建触发事件就会请求播放。
- 页面停止即停止采集和分析；当前没有前台服务、开机启动或后台持续监测能力。

本文件后文的阈值均是当前代码值，不代表已经通过道路法规、OEM 量产验收或功能安全认证。

## 1. 项目范围

在 SM4450 / Android 16 车机上复用已有 USB 前视摄像头，开发独立 Java Android APK，最低运行环境为 Android 13（API 33）。当前交付功能包括 FCW 前向碰撞预警、HMW 前向车距监测和 LVSA 前车起步提醒；LDW 保留为后续可选能力。

- 不接入 CAN/LIN、毫米波雷达和超声波传感器。
- 不修改 AOSP、Vendor、HAL、SELinux 和 init。
- 独立使用现有 libusbcamera.aar，不修改录制应用业务。
- 视觉结果用于提示，项目不提供制动控制或自动驾驶能力。
- CPU ≤5%、RSS ≤80 MB、APK ≤15 MB 为原始规划目标，尚未全部实现或验证；CPU 统计标尺必须明确，RSS 与 PSS 分别记录。

## 2. 已有证据与可行性

| 方面 | 已知事实 | 当前结论 |
|---|---|---|
| USB 输入 | 现有 UVC 库能够输出 NV21；用户实测采集最高 31 FPS | 输入链路具备继续开发条件，长期稳定性仍需验收 |
| 车辆检测 | LiteRT 1.4.1、EfficientDet-Lite0 UINT8、320×320 输入，用户实测推理 60–90 ms | 检测原型可运行，不等于完整 ADAS 报警已验证 |
| CPU | top 进程 89.6%，设备总标尺 800% | 约 11.2% 的整机 CPU 时间占比；不能称为整机占用 89.6% 或等效算力占比 |
| 内存 | 详细 meminfo：PSS 114476 KB，RSS 255754 KB | 约 111.8 MiB / 249.8 MiB；未达到原 RSS 80 MB 目标 |
| 内存变化 | 两个相隔约 13 分 21 秒的采样点，PSS 相差 256 KB | 未见明显增长趋势，不足以证明长期无泄漏 |
| 无总线方案 | 可使用视觉估计相对运动、GPS 提供速度门限 | 受标定、地形、遮挡和 GPS 有效性约束 |
| 工程形态 | Java/Gradle 独立工程已建立，用户确认上一版编译运行正常 | 后续按小步开发、逐轮编译实测推进 |

这些数据来自新增跟踪、标定、运动量和报警接入之前的程序。本轮目标选择/跟踪、标定模型/保存、距离/TTC、GPS 速度、LDW 原型、固定音频及用例已写入，编译、测试执行和设备效果尚待验证。

## 3. 当前架构与技术选择

~~~text
USB 摄像头
  → libusbcamera.aar / 原生 UVC
  → UsbCameraSource（每隔至少 200 ms 复制采样帧）
  → FrameDispatcher（容量 2，满载丢旧帧）
  → FrameConsumer（独立消费线程）
  → NV21 转 RGB、等比例缩放与补边
  → LiteRT / EfficientDet-Lite0（320×320、1 推理线程）
  → car / bus / truck 检测框
  → LeadVehicleTracker（前方候选、匹配、ID、漏检与切换）
  → 检测/跟踪快照 → UI
~~~

当前默认分析输入上限约 5 FPS（采样节流 200 ms），实测采集最高 31 FPS，因此约 84% 的采集帧不执行推理。60–90 ms 的“单帧推理”只覆盖预处理、模型调用和检测后处理，不包含跟踪、单目测距与决策，也不包含完整采集、排队及显示延迟。5 FPS 下 FCW 三个危险采样的首尾跨度约 400 ms，还需加上采样相位、目标确认和处理延迟，不能将端到端延迟固定称为 600 ms。

Java 使用库提供的原生能力，未自建 JNI/NCNN 算法层。NV21 原生缓冲在回调结束后复用，因此当前采样帧会复制到 Java 数组；不能称为零拷贝或零分配。模型输入和输出缓冲已经复用。是否增加缓冲池、更换模型或引入其他后端，应根据独立对比数据决定。

## 4. 核心功能原理和限制

### 4.1 前方候选选择与跟踪

在梯形搜索区筛选底边中心，按底边位置与居中程度选择候选。有效标定且尺寸匹配时，区域上边跟随标定地平线，避免大俯仰角漏掉远车；无有效标定时回退 y=0.45。横向中心保持 0.5。通过框重叠率、中心位移和面积比例关联前后帧，连续确认后分配 ID。

短暂漏检只保留关联历史，不输出旧框作为当前观测；超时或确认换车后使用新 ID。多个框匹配接近时拒绝直接继承旧 ID。具体初始阈值和实测场景见最终方案第 4 节。

这个搜索区不是本车道检测；弯道、相机偏置、交叉遮挡可能造成误选或误关联，后续须结合标定、车道信息和实车验证。

### 4.2 摄像头标定：紧凑设置页＋双线安装校准

当前 V1 不要求车主测量距离，也不要求摄像头提供序列号。设置页只选择车型和镜头 HFOV，进入校准后在实时画面上拖动两条参考线：水平线反算俯仰角；竖直线保存为后续固定车道图形的中心，不校正 yaw、光学主点或前车归属。俯仰角限制在 `2°~14°`，中心线偏移限制在 `±0.12`；超出范围显示红色且不能确认。保存后按当前画面分辨率写入 `DISTANCE_READY`，分辨率变化时需要重新校准。校准从有效 UVC 帧读取尺寸和会话信息，不依赖推理成功；VERSION 1 旧配置升级后需重新确认双线。

下方“行驶自学习”段落是历史车道能力设计，当前 `LDW_ENABLED=false` 时不执行，不影响前车测距和告警。

以下内容记录早期“静态安装向导＋行车灭点自动学习收敛”的设计推导，便于后续恢复 LDW 时参考；当前 V1 只执行上面的双线校准，不进入这些自动学习步骤：

1. **静态向导阶段**：
   - **离地高度 $H$**：通过车型预设选择（轿车 1.25m、SUV/MPV 1.45m、货卡 1.75m 或自定义数值）粗略确定。
   - **镜头内参（$f_y, c_y$）**：基于标准 USB 镜头规格（默认 90° HFOV，在 720P 下等效 $f_y \approx 640\text{px}$）或预置参数表，主点 $c_y$ 初值取画幅中心。
   - **初始俯仰角 $\alpha$**：向导提供 $2^\circ\sim14^\circ$、每 $0.5^\circ$ 一档的受控滑块，默认 $8.0^\circ$；已有配置会回填当前值。运行时仍由 ROI/地面射线守卫按实际高度判断，镜头明显朝向天空或机盖时距离能力保持静默。
   - **机械对齐引导**：UI 叠加“机盖参考线”与“地平线目标带”，引导安装时将镜头调平拧紧。
2. **动态自学习阶段（行车灭点估计与收敛）**：
   - **数学原理**：真实帧主路径使用多行车道宽度比，旧灭点路径仅作兼容回退。行射线角 $\beta=\arctan((y-c_y)/f_y)$ 时，地面距离为 $Z_{ground}=H/\tan(\alpha+\beta)$，相机光轴深度为 $Z_{axis}=H\cos^2\beta/\sin(\alpha+\beta)$，车道宽度契约为 $w=Wf_x\sin(\alpha+\beta)/H$；灭点路径仍满足：
     $$\tan\alpha = \frac{c_y - y_{vp}}{f_y} \implies \alpha = \arctan\left(\frac{c_y - y_{vp}}{f_y}\right)$$
   - **特征提取与门控**：在 GPS 车速 $35\sim 120\text{ km/h}$、车道中心偏移不超过 0.15 且车道质量达标时，利用 `LaneDepartureDetector` 提取左右车道线斜率方程求解交点 $y_{vp}$。求解前还有一道 ROI 可见性守卫：车道采样带固定在归一化行，俯仰角越大其覆盖的地面距离越靠近车头，远带边沿不足 4 m 时判定采样已进入机盖区域并丢弃，避免用不可信野值收敛 pitch。当前没有横摆率、方向盘角度或加速度输入，不能声称已经过滤所有弯道、颠簸和主动变道。
   - **收敛判定与持久化**：当前实现优先使用 9 行宽度样本的近/远比值，仍保留 60 个有效样本的滑动窗口和 VP 兼容回退；宽度主路径使用 implied pitch 的 MAD≤0.40°，没有实现卡尔曼滤波，也没有校正完整内参、畸变、yaw 或横向安装偏差。
3. **慢速跟踪与安全降级**：
   - 收敛前系统处于 `CALIBRATING` 状态，测距预警保守静默；
   - 收敛后进入 `CALIBRATED`，仅在图像尺寸完全匹配时开放当前平面模型的测距输入，并以极低增益（$\alpha_{track} = 0.002$）慢速跟踪 pitch 漂移。该状态不等于完成可量产相机标定；尺寸改变会使测距失效，当前没有自动重新绑定摄像头身份或校正其它姿态参数。
### 4.3 FCW/HMW：距离、相对速度与 TTC（已接入，待设备验证）

理想平面道路模型下，设相机离地高度为 \(H\)，向下俯仰角为 \(\alpha\)，竖直焦距为 \(f_y\)，主点纵坐标为 \(c_y\)，前车接地点为 \(y_b\)：

\[
D = \frac{H}{\tan\left(\alpha+\arctan\left(\frac{y_b-c_y}{f_y}\right)\right)}
\]

只有坐标系、单位、图像裁剪和相机参数一致时，公式才有意义。检测框底边只是接地点的近似；遮挡、底边截断、坡道和车身震动会引入误差。仅对齐机盖边缘不能自动完成完整标定。

在同一有效目标上，使用真实时间间隔估计距离变化和逼近速度 \(v_c\)，当 \(v_c>0\) 时计算 \(TTC=D/v_c\)。目标切换、失效或长时间间断时清除历史，避免距离跳变制造虚假逼近速度。

`LeadVehicleMotionEstimator` 已将同一目标 ID 的距离、逼近速度和目标面积送入 `AdasDecisionEngine`；无标定、尺寸不匹配、ID 丢失、进入 `LOST` 或几何无效时测距静默并清除速度历史。FCW 还要求有效自车速度。HMW 已加入 THW，但同时保留 8 m / 4 m 固定距离兜底；停车时只抑制极近音频，普通 HMW 视觉提示仍可能显示。滤波不能消除错误标定或坡道造成的系统性误差。

### 4.4 LVSA：前车起步提醒（已接入，待设备验证）

需要可靠的自车静止状态与连续前车观测。GPS 速度缺失、过期或精度不足时，不能直接按 0 km/h 处理。前车距离增加、面积减小仅作为运动证据，仍须区分目标切换、遮挡和检测抖动。

当前规则要求目标在 3–9 m、自车速度不超过 1 km/h、逼近速度绝对值不超过 0.75 m/s，并先稳定等待 3 s；随后距离增加至少 2.5 m 或目标面积连续下降至少 15% 且满足 2 次采样才触发 LVSA，冷却 10 s。没有有效速度时不会把状态假定为静止。静止时降低分析频率属于后续可评估策略，当前没有实施。

### 4.5 LDW：车道偏离（轻量原型已接入，待设备验证）

当前原型在 NV21 的 Y 分量 ROI 中搜索左右亮边缘并计算中心偏移，配合连续置信度、偏离持续时间、有效速度和报警冷却；它还不是完整的直线拟合或车道跟踪实现。

当前决策门限为自车速度 ≥50 km/h、车道置信度 ≥0.35、归一化中心偏移绝对值 ≥0.12，并持续 1 s；事件冷却 6 s。LDW 已受标定门控：只有标定状态为 `CALIBRATED`（且画面尺寸与标定绑定一致）时才把车道观测交给决策引擎，未完成标定时静默 LDW；室内模拟为验证场景需要而不受该门控。真实算法固定在 NV21 Y 分量 ROI 中寻找亮边缘，室内模拟直接注入可用 Observation，两者不能互相证明。车道线不可见或速度无效时静默相关报警；没有转向灯及整车信号，单靠视觉不能保证区分主动变道和无意偏离。

### 4.6 报警与生命周期（固定音频已接入，后台能力待实现）

固定短音频已通过 `SoundPool(USAGE_MEDIA)` 接入，并以 `ToneGenerator(STREAM_MUSIC)` 作为软件音调回退；FCW 优先于 HMW_CRITICAL、LDW 和 LVSA，高优先级事件会主动停止已播放的低优先级流。仍没有 AudioFocus、独立 ADAS 通道、硬件蜂鸣器或正式构建总开关。事件产生后会直接请求播放，TTS 尚未接入。当前页面停止时会停止采集/消费，并不具备持续后台 ADAS 能力。

### 4.7 场景判定矩阵（以当前代码为准）

| 场景 | 必要输入与门控 | 当前触发规则 | 无效/丢失时行为 | 输出与冷却 |
|---|---|---|---|---|
| FCW | 目标 `TRACKING` 且可见；有效安装配置（当前为 `DISTANCE_READY`）；尺寸匹配；GPS 速度有效 | 自车速度 ≥20 km/h，距离 >0，逼近速度 >0，`distance / closingSpeed ≤ 2.4 s`，连续 3 个分析帧且时间跨度 ≥200 ms | 目标丢失、速度/距离/逼近速度无效或时间间隔过大时清零危险帧 | `FCW` 事件；3 s 冷却 |
| HMW 普通 | 目标可见，距离有效 | 速度 ≥15 km/h 时 `THW≤1.2 s 或距离≤8 m`；更低速按距离≤8 m | 目标/距离无效则不显示普通 HMW；停车时仍可显示 ≤4 m 的普通视觉提示 | `headwayWarning` 持续为真；无声音事件和冷却 |
| HMW 极近 | 目标可见，距离有效；逼近速度未显示拉开（NaN 或 ≥−0.5 m/s） | 自车速度 ≤1 km/h 静默；速度 ≥15 km/h 时 `THW≤0.6 s 或距离≤4 m`；更低速按距离≤4 m | 自车静止、目标/距离无效时不触发事件 | `HMW_CRITICAL` 事件；4 s 冷却 |
| LVSA | 目标可见且连续同 ID；GPS 速度有效且 ≤1 km/h；距离和面积有效 | 目标距离 3–9 m，先稳定 3 s；之后距离增加 ≥2.5 m 或面积下降 ≥15%，连续 2 帧 | 容许 600 ms 短暂目标丢失；目标切换会清空决策状态 | `LVSA` 事件；10 s 冷却；无效速度不按 0 处理 |
| LDW | 车道 `available`，速度有效；要求标定 `CALIBRATED` 且画面尺寸与标定绑定一致 | 速度 ≥50 km/h，置信度 ≥0.35，中心偏移 ≥0.12，持续 ≥1 s | 未标定、车道不可用、置信度/偏移/速度无效会清零持续计时 | `LDW` 事件；6 s 冷却。没有转向灯抑制 |

表中的“连续帧”依赖当前约 200 ms 采样和实际推理耗时，不能直接作为量产响应时间指标。`MainActivity` 还会把最近事件在 UI 保持约 1.5 s；保持集合绑定目标 ID，目标切换或丢失时会清空，不会继承旧目标的事件颜色或风险文字。

### 4.8 量产前必须关闭的链路缺口

- GPS 已校验速度字段、时间戳和 2 s 新鲜度，并过滤水平精度 >50 m、速度 >180 km/h 和短间隔加速度 >15 m/s²；没有 provider 优先级和位置跳变仲裁。车速链路要求 FINE 权限，只有 COARSE 时停用车速相关功能。
- 标定只按当前画面分辨率判断几何参数是否适用，不读取或绑定 USB 序列号；更换摄像头、改变安装位置或改变分辨率后必须重新执行双线校准并进行实车复测。
- 距离差分启动时先确认相邻原始差分同向且变化符合 12 m/s² 约束，再启用速度限幅滤波，避免单次或交替回摆长期残留；目标 `LOST`、ID/时间/标定变化时清空历史。真实噪声、急刹响应、遮挡和换车仍需实测。
- USB 打开阶段有 8 s watchdog，视频流确认后有 3 s 无帧 watchdog；相机与 monitor 在相机线程关闭，SurfaceTexture 由 listener 在相机线程释放并返回 `false`，由 listener 单独承担释放责任。原生调用自身若阻塞，仍受 AAR 行为限制。
- LiteRT 初始化失败保留 5 s 重试；推理运行时异常会先关闭失败解释器，再按 5/10/20/40 s 退避重试。音频满 4 路时也会先停止最早流，随后高优先级事件停止所有已跟踪低优先级流。以上问题仍需设备级故障注入和长期运行验证。

### 4.9 证据分级

当前工作区有 15 个 JVM 测试类、147 个 `@Test` 方法，覆盖纯 Java 跟踪、几何、决策、队列、模拟场景、USB 契约和速度历史清理；历史临时 `javac + JUnit` harness 曾通过 106 个纯 Java 用例，但 Gradle focused suite 受环境 loopback 错误阻断。没有 Android instrumented 测试，也没有真实 NV21 车道帧、UVC 热插拔、LiteRT 真模型、音频出声或 Activity 重建测试。`AdasSimulator` 的 LDW 直接注入可用 Observation，不能替代真实图像验证。用户确认编译正常不等于本轮代码和实车验收通过。

## 5. 实施与验证顺序

1. 前方候选选择。
2. 连续跟踪、漏检和换车。
3. 摄像头标定及参数保存。
4. 单目距离估算。
5. 距离平滑、相对速度及 TTC。
6. GPS 有效速度与 FCW/HMW/LVSA 接入，显示状态。
7. 对已接入的 LDW 和声音原型完成真实道路、音频通道和故障恢复验证，再进行长期稳定性收口；转向灯、横摆率、方向盘角度和弯道补偿留待后续版本。

本轮范围覆盖第 1–7 点的 Java 原型链路和文档同步，后台服务、TTS 和完整车道线拟合仍未实现。用户负责编译；新增测试尚未执行。后续需要已知距离的静态场景、相机安装参数及实际道路数据，才能验证测距和报警效果。原型可运行不等于稳定版本验收。

# HCT ADAS V1 精简版：车道感知、标定与输出规格

> 文档基线：2026-09-20 工作区。当前统计为 **18 个 JVM 测试类、192 个 `@Test` 方法**；历史 Gradle focused suite 曾受环境 loopback 错误阻断，历史临时 `javac + JUnit` harness 曾通过 106 个纯 Java 用例，因此不把统计误写成“全量通过”。
> 对标对象：消费级后装 ADAS（行车记录仪 ADAS / Mobileye 后装 / Tesla 摄像头校准流程）。
> 本文是当前 V1 轻量车道与标定实现的基线；V2 双模型和 ByteTrack 模块已移除。
> 设备与实车验证仍未完成；所有阈值都是待实车确认的工程初值。

> **当前落地边界（2026-09-19）：** 为避免实车自动车道标定长期停在 0/60 或少量样本，当前版本关闭 LDW 运行分支和实时车道叠加显示。紧凑设置页只保留车型、镜头 HFOV 和 ADAS 校准；校准页通过水平线、竖直线做有限范围安装对齐，不绑定摄像头序列号，也不显示中央安装提示。保存后按当前分辨率进入 `DISTANCE_READY`；前车距离、FCW、HMW、LVSA 仍按本文的目标跟踪与规则链路运行。车道章节保留作后续升级设计，不作为本版验收条件。

> **固定视觉参考（2026-09-20）：** [固定透视参考带设计规格](HCT_ADAS_Fixed_Guide_Design.md) 已接入代码，使用已保存的竖直中心和地平线绘制固定模板，颜色复用前车风险；支持速度显隐和箭头动效。未编译或进行设备验收，与下文保留的实时车道检测、曲率及 LDW 设计分别管理。

## 1. 最终决策摘要

| 决策项 | 结论 | 理由 |
|---|---|---|
| 是否引入 YOLO/UFLD 车道模型 | **不做**（保留为后续可选升级） | 当前故障是标定判据数值病态，不是感知能力不足 |
| 是否引入 ByteTrack | **不做**（先做单目标"预测 + 丢失缓冲"轻量升级） | 5 FPS 采样下 ByteTrack 的运动模型收益有限 |
| 采样帧率 | **不提升**（保持约 5 FPS 分析） | 单帧 86 ms 已实测，双模型将超出预算 |
| 当前标定判据 | **双线安装校准 + 实时画面/会话/尺寸校验** | 水平线反算俯仰，竖直线保存视觉中心；第 3 节车道自学习仅为保留设计 |
| 用户操作 | **车型/HFOV 选择 + 双线安装校准** | 只需把水平线对齐地平线、竖直线对齐车头中心；超出范围禁止保存 |
| 当前驾驶员 UI | 正常运行显示时间、车速、设置入口；前车距离跟随真实目标框显示；告警以目标标签和报警音表达 | 已删除底部固定距离卡片、三段读数和车道数值叠加；LDW/实时车道 UI 不属于本版验收；固定参考带按独立规格验收 |

## 2. 保留 / 修改 / 新增

**保留架构**：采集链路（`UsbCameraSource` / `FrameDispatcher` / `FrameConsumer` / `Nv21Preprocessor`）、
`LiteRtVehicleDetector` + EfficientDet-Lite0、`AdasDecisionEngine` 全部判定阈值与状态机、
`LeadVehicleMotionEstimator`、`CameraCalibration` 的平面测距模型、`AlertAudio`、以及"未标定即静默"的门控原则。

**修改**：

| 文件 | 改动 |
|---|---|
| `LaneDepartureDetector` | 多行采样（9 行，ROI 行 0.54~0.82）+ 车道线身份锁定（从画面中心向内取脊）+ 行内中值亮度归一化 + 输出 `widthSamples` |
| `LeadVehicleMotionEstimator` | `LOST`/新 ID 清空速度历史；首差分临时输出，相邻原始差分一致后才初始化限幅，启动回摆不锁成持续运动 |
| `LeadVehicleTracker` | 有效标定地平线决定纵向搜索起点，未标定回退 y=0.45；不使用竖直视觉中心作为光学主点 |
| `UsbCameraSource` | 带会话编号的有效预览快照供校准使用；显式释放原始 USB 控制块；SurfaceTexture 单次释放并返回 `false`；保留 watchdog 与多格式协商 |
| `MainActivity` | LiteRT 运行时异常指数退避；车速链路要求 FINE，COARSE 仅用于提示升级权限，不作为有效车速来源 |
| `AlertAudio` | 高优先级报警会停止已有低优先级 SoundPool 流和 ToneGenerator；`stop()` 清理优先级锁 |
| `AutoCalibrationLearner` | 以车道宽度近/远比值为观测量反解俯仰；驾驶类拒识改为**暂停不清零**；"安装角度超限"只在 ROI 真的看不到路面时提示 |
| `MainActivity` | 紧凑设置页、双线校准、距离能力门控、前车状态与运行异常提示；车道学习分支由产品开关关闭 |
| `VehicleOverlayView` | 生产构建只绘制当前跟踪目标框及其距离/告警标签；车道绘制代码保留在关闭的后续分支 |
| `AdasSimulator` | 合成观测带宽度样本，使室内模拟走真实解算路径（自标定场景故意比配置角陡 1.5°） |

**保留的后续能力**：`LaneGeometry`、`LaneDepartureDetector`、`AutoCalibrationLearner` 仍在仓库中，用于后续车道能力恢复；`AdasUiStatusMapper` 已删除，当前告警标签直接由目标叠加层和决策事件生成。

## 3. 核心解法：俯仰的观测量与持久化值

### 3.1 旧判据为什么必然失败

旧实现用两条相隔 0.18 的图像行外推灭点，要求灭点落进 `[0.30, 0.60]`：

```
vpY = 0.78 − 0.18 · wB/(wB − wT)
```

真机地平线约在 0.34 附近，容差只有 0.04，反推回像素约 11 px；而检测器 x 步进 2 px、每带仅 10 行，
量化误差恰好就是这个量级。结果：一半帧被拒 → 每拒一次 `resetSamples()` → 进度永远 0% →
几何拒识计数累积 → 误报"安装角度超限"。

### 3.2 正确的地面投影模型（本轮修正的关键）

对行射线角 `β = atan((y − c_y)/f_y)`，测距使用地面前向距离 `Z_ground = H/tanθ`，其中 `θ = α + β`；
车道宽度沿当前校准契约写作：

```
w_norm = W · f_x · sin(θ) / (H · cosβ)  θ = α + β,  β = atan((y − c_y)/f_y)
```

横向米制投影直接使用相机光轴深度：`X = (x − 0.5) · Z_axis / f_x`。独立契约先定义
`Z_axis = H·cosβ/sin(α+β)`；测距 API 仍返回
`Z_ground = H/tan(α+β)`。三者不混用，也不再用 `Z·cos(pitch) + H·sin(pitch)` 拼装第二个深度。
独立横向测试使用 `Z = H·cosβ/sin(α+β)` 与
`x = 0.5 + f_x·X/(Z·cosβ)` 两条关系；宽度样本与曲率样本也分别按独立深度生成，避免复用生产函数。

于是**近/远两行的宽度比**：

```
R(α) = sin(θ_near) / sin(θ_far)
```

这个比值的性质（也是选它作观测量的原因）：

1. **与车道宽先验 W 无关** → 车道宽度假设偏差不进入结果；
2. **与相机高度 H 无关** → 车型预设选错不影响；
3. **与焦距 f_x 无关**（`β` 的高阶项除外）→ 内参 HFOV 未标定也不影响；
4. **对俯仰敏感**：ROI 行 0.82/0.54 下，俯仰每变 1° 比值变化约 8~12%（4°→12° 时比值由 3.52 单调降到 2.09）。

因此用**二分法**在 `[配置角−6°, 配置角+6°]` 内反解 α_near，即可得到"这帧看起来的俯仰角"。

### 3.3 两个必须遵守的边界条件（实现中已加）

- **视场下限**：远行在俯仰过低时会越过地平线，`θ_far` 变号，比值不再单调。求解区间下界必须钳在
  `horizonPitchDegrees(ROI_TOP_ROW) + 1.2°` 之上，否则会解出无意义的角或直接失败。
- **近行不能太靠下**：近行越靠画面底部，其俯角越大，比值模型在低俯仰时越容易失去单调性。
  ROI 近端因此定在 0.82（而不是 0.86），实车机盖线仍在 0.9 以下。

### 3.4 谁决定最终写入的俯仰角

**配置角是持久化值，观测量只负责"确认"**：

- 窗口内 `α_near` 的中位数与配置角之差超过 `MAX_PITCH_DEVIATION_DEGREES = 4°` → UI 提示"镜头俯仰需 ±N°，请小幅调整支架"；
- 差值在 4° 以内 → 判为确认，写入**配置角**（不含观测量自身的先验偏差）；
- 收敛判据是窗口内 `α_near` 的**中位绝对偏差 MAD ≤ 0.4°**（不是标准差：一帧野值不能重置整个窗口）；
- 锁定后只做 `ONLINE_TRACKING_RATE = 0.002` 的慢跟踪，且单帧观测量与锁定值相差超过 4° 时直接忽略，
  防止用先验把已经标定好的角拉走。

## 4. 消费级标定流程（用户侧）

```
① 紧凑设置页               选择车型与镜头 HFOV
② 双线校准                 水平线对齐地平线，竖直线对齐车头中心
③ 有效性检查               俯仰 2°~14°、中心偏移 ±0.12，越界显示红线并禁止确认
④ 保存                     按当前分辨率写入 `DISTANCE_READY`，开放前车距离与 FCW/HMW/LVSA
⑤ 后续行驶                 当前不做自动车道学习；LDW 保留为后续可选能力
```

两条线按原始图像的归一化坐标保存；预览、目标框和校准页共享避开导航栏的等比例画幅，黑边不参与拖动。竖直线仅作为后续固定车道参考图形的中心，当前不绘制固定车道，也不校正 yaw 或光学主点。校准要求有效 UVC 帧，不依赖模型结果；相机断开、会话/尺寸变化或返回/退出时取消未保存配置。旧 VERSION 1 配置升级后需重新确认双线。

## 5. 输出规格

### 5.1 前车碰撞预警 FCWS

| 条件 | 文本 | 颜色 |
|---|---|---|
| 无前车 / 速度无效 | `Determined …` | 黄 `#FFD54F` |
| TTC > 2.4 s 且无 HMW 条件 | `Normal Risk` | 绿 `#00E676` |
| TTC ≤ 2.4 s，或 THW ≤ 1.5 s，或 HMW 条件 | `Prompt Risk` | 橙红 `#FF7043` |
| TTC ≤ 1.2 s，或 collisionDanger / HMW_CRITICAL | `Warning Risk` | 红 `#F44336` |

`AdasDecisionEngine` 的 FCW/HMW/LVSA 判定链路保持独立；当前版本不启用 LDW 的驾驶决策和车道 UI。

### 5.2 车道偏离警告 LDWS

```
Offset = 车辆中心相对车道中心的横向距离（米）
         车辆在车道中心右侧为正（此时提示向左修正），左侧为负
R      = 车道线平均曲率半径（米，Z = 15 m 处求值）
```

| 条件 | 文本 |
|---|---|
| \|Offset\| ≥ 0.12 车道宽且持续 1 s | 车辆在右 → `Please Keep Left`；在左 → `Please Keep Right` |
| 其余 | `Good Lane Keeping` |

### 5.3 车道保持辅助 LKAS

以 R 的绝对值分级、符号定左右（世界坐标系、ISO 8855：左弯为负）：

| 条件 | 文本 |
|---|---|
| 未锁定 / R 不可用 | `To Be Determined …` |
| \|R\| > 500 m | `Keep Straight Ahead` |
| 200 m < \|R\| ≤ 500 m | `Gentle Left/Right Curve Ahead` |
| \|R\| ≤ 200 m | `Hard Left/Right Curve Ahead` |

R 由地面前向距离上的二次拟合 `X = a·Z_ground² + b·Z_ground + c` 得出：`R = (1+(2aZ_ground+b)²)^1.5 / (2a)`，
Z_ground = 15 m 处取值；`|a| < 1e-4` 判为直行（返回"无曲率"而不是一个巨大的半径）。负半径表示 Left，正半径表示 Right。

## 6. 实现状态（本轮完成）

**当前工作区静态统计：18 个 JVM 测试类、192 个 `@Test` 方法**。历史临时 `javac + JUnit` harness 曾通过 106 个纯 Java 用例，但 Gradle 未能启动，不能宣称全量用例已通过。现有覆盖还包括：
`LaneGeometryTest` 的独立投影、比值反解、偏移符号、曲率符号与直行判定；
`LaneDepartureDetectorTest` 的同侧亮线竞争与缺端点样本；
`LeadVehicleMotionEstimatorTest` 的 `LOST`/重获速度历史清理；
`UsbCameraSourceTest` 的候选格式和 SurfaceTexture 释放契约；
以及 `AdasLogFormatTest`、`AutoCalibrationLearnerTest` 的既有几何、状态和日志覆盖；已删除的 `AdasUiStatusMapperTest` 不再属于当前测试集。

代理曾用 `javac + JUnit` 独立跑过本轮重点用例（车道 12、运动 4、跟踪 19、USB 契约 5）；几何公式在此后又做过收口，因此这些结果不能替代当前几何改动的重跑。这不等同于 Android/Gradle 全量测试通过，环境中的 Gradle 启动仍受 loopback 错误阻断。

| 项 | 状态 |
|---|---|
| `LaneGeometry`：投影、宽度、偏移、曲率、比值反解、视场钳制 | 已实现 |
| `LaneDepartureDetector`：9 行采样、身份锁定、局部归一化、宽度样本 | 已实现 |
| `AutoCalibrationLearner`：比值观测量、暂停语义、拒识归因、MAD 收敛 | 已实现 |
| `AdasUiStatusMapper`：FCWS/LDWS/LKAS 状态映射 | 已删除；当前由决策事件直接驱动目标标签 |
| `AdasSimulator` + 室内模拟场景走真实解算路径 | 已实现 |
| UI：目标框旁距离/告警标签、时间、车速、无背景设置入口 | 已实现；底部固定距离卡片、中央安装提示和车道 Offset/R 读数已删除 |
| 安装校准：车型/HFOV 选择、水平线/竖直线双线校准、2°~14° 和 ±0.12 边界 | 已实现 |
| 叠加层：走廊从机盖参考线起画；丢线 250 ms 内保持实线，之后降级为灰色虚线 | 已实现 |
| 诊断日志：`[CALIB]` 1 Hz 心跳、`[LANE]` 逐行采样、`[LANE-DRAW]` 绘制样式 | 已实现 |
| 推理运行时异常退避、精确定位权限门控、音频高优先级抢占 | 已实现，设备行为未验证 |
| USB Surface 生命周期、分辨率绑定、打开/断流 watchdog、多格式协商 | 已实现，真实 UVC 行为未验证 |
| 设备与实车验证 | **未做** |

## 7. 实车验证要点

1. 观察进度条是否随有效帧上涨（不再是恒 0%）；
2. 抓 `[CALIB]` 日志：`measuredRatio` / `modelRatio` / `implied` / `spread` / `laneWidth`，
   其中 `laneWidth` 是内参与高度的自检量：3.0~4.0 m 为正常，明显偏小说明 HFOV 填小，6~7 m 说明锁到邻道线；
3. 用已知车道宽的直路核对 Offset 的符号与量级；用已知半径的弯道核对 R；
4. 记录单帧耗时（`[HEARTBEAT] Split=` 的第三段）、FPS、PSS/RSS，确认多行采样没有吃掉实时性预算；
5. 复核"调整俯仰"提示是否只在真装歪或采样区真的看不到路面时出现；
6. `[LANE-DRAW] style` 应以 `solid` 为主且不来回跳；`[LANE]` 应逐行给出左右边界。

## 8. 明确不做的事（及重启条件）

| 不做 | 重启条件 |
|---|---|
| 引入 YOLO/UFLD 车道模型 | 实车显示车道线**整段丢失**（夜间、阴影、磨损标线）且时序与亮度改造无法覆盖时 |
| 引入完整 ByteTrack | 需要"旁车道车辆稳定 ID"时 |
| 提升分析帧率 | 感知与标定正确性收口后，用于改善 FCW 触发延迟时 |
| 引入多任务单模型（YOLOP 类） | 双模型实测超出时间预算时 |
| yaw / 横向主点标定 | 显示数值需要更高绝对精度时；当前比值观测量已规避其影响 |

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 横向主点假设在画面几何中心 | 双侧车道线共模误差在差值中抵消 |
| 车道宽先验与实际不符 | 观测量是**比值**，先验只用于合理性检查（2.6~4.8 m 之外判为锁错边界） |
| 反解被野值带偏 | 4° 漂移门 + MAD 判据 + 中位数统计 |
| 同侧接缝/邻道线抢边界 | 上一帧位置 ±0.07 搜索窗 + 位置连续性优先于亮度；超过 0.035 的跳变需连续 3 帧确认，冷启动才全半幅搜索，仍需真实道路验证 |
| 邻道线误锁 | 跨帧连续性 + 宽度合理性 + 多行样本门控；不宣称能覆盖所有遮挡/施工线场景 |
| 极端俯仰导致采样区看不到路面 | ROI 可见性守卫 + 明确提示调支架 |
| 低俯仰时比值失去单调性 | 求解区间按远行地平线钳制；ROI 近端上移到 0.82 |
| 许可与合规 | 不引入新模型权重，无新增许可负担 |

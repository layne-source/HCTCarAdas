# HCT ADAS V1 固定透视参考带设计规格

> **版本与日期：** v1.2，2026-09-21（v1.1 设计定稿于 2026-09-20）
>
> **开发基线：** `bff24dd945978a2c4d9d90ce17fdcafe4f58ef93`；固定参考带、三类声音开关和设置弹窗在该基线收口。
>
> **状态：** 代码已接入并已提交；用户已确认编译、权限、室内模拟、设置弹窗和声音开关可用。道路识别精度、长稳、声压和音频通道仍按设备/实车验收边界记录。
>
> **适用范围：** V1 精简前车版；SM4450；Android 13/API 33 及以上；当前 720p 摄像头及后续 1080p 摄像头。

## 1. 目的与产品边界

在真实摄像头画面上增加固定的透视参考带：从车头附近向前收拢、逐渐透明，以绿、黄、红表达前车监测状态，并在合适条件下显示向前移动的箭头。车速达到门槛后显示，低速时隐藏。

“固定车道”在本文中统一称为“固定透视参考带”。它的形状由安装校准和视觉模板确定，不依赖逐帧车道识别，不随前车框位置摆动，也不在弯道上自动转弯。其宽度不代表测得的真实车道宽度，箭头不表示导航路线、转向控制或允许加速。

本次范围保留现有 EfficientDet-Lite0/LiteRT、前车跟踪、测距及 FCW/HMW/LVSA。`AdasCalibrationMode.LDW_ENABLED` 继续为 `false`，固定图形独立接入，不恢复 `LaneDepartureDetector`、车道学习或 LDW，不引入 V2 模型及三维渲染引擎。

**显示门槛仅作用于参考带及箭头。** 前车框、距离、低速 HMW、停车 LVSA、FCW 和报警音仍按原有条件运行。设置页包含车型、镜头 HFOV、ADAS 校准和 FCW/HMW/LVSA 三个即时声音开关，不增加参考带调参入口或新的校准线；开关不需要保存/取消按钮，弹窗外点击关闭，选项内容内部滚动。

## 2. 本次确定的设计及参数

| 项目 | v1.0 设计 | 性质 |
| --- | --- | --- |
| 几何 | 下宽上窄、中心固定、远端向校准地平线收拢 | 产品要求 |
| 透明度 | 近端填充 25%，中部约 12%，远端 0%；边缘也随距离渐隐 | 视觉初值，设备上验证 |
| 三色 | 绿 `#36D99C`、黄 `#F2BA49`、红 `#FF525E` | 视觉初值，风险语义固定 |
| 箭头 | 3 个细小 `∧` 形箭头；从近端向远端移动、缩小并渐隐 | 产品要求 |
| 动画周期 | 1.8 秒一轮；不随 GPS 瞬时速度加快 | 工程初值 |
| 显示门槛 | 有效车速 ≥20 km/h 连续 1,000 ms | 工程初值 |
| 隐藏门槛 | 有效车速 ≤15 km/h 连续 1,000 ms | 工程初值 |
| 中间区间 | 15 < v < 20 km/h 保持原显示开关状态，清除未完成的门槛计时 | 固定状态规则 |
| 动画频率 | 目标约 25～30 FPS，上限 30 FPS；模型分析保持原采样策略 | 实施约束 |
| 数据异常 | 隐藏参考带、停止箭头；异常恢复后重新满足显示条件 | 固定状态规则 |

以上视觉及速度参数是本项目首版选择，不是行业统一标准或已经完成实测的量产指标。后续如需调整，应同步本文及对应验收用例，不在 UI 中散布重复常量。

## 3. 成熟产品参考及借鉴范围

| 公开资料 | 可核实内容 | 本项目借鉴范围 |
| --- | --- | --- |
| [大众 ID.3/ID.4 AR-HUD 官方介绍](https://www.volkswagen-newsroom.com/en/press-releases/from-the-luxury-class-to-the-compact-segment-the-augmented-reality-head-up-display-6730) | 使用与现实场景叠加的箭头和标记；导航箭头随接近路口变大并淡化纹理以保留道路视野；Lane Assist 边界提示为橙色，Travel Assist 启用时出现绿色线，危险接近前车可显示红色警告 | 借鉴透视层次、透明处理、信息克制与风险颜色；不复制其车道控制功能含义 |
| [Mobileye HMW 官方介绍](https://www.mobileye.com/us/fleets/technology/headway-monitoring-warning/) | 以秒为单位监测与前车的时间间隔，低于设定阈值时提醒 | 继续复用本项目现有 THW/HMW 与 FCW 结果，不为参考带重新定义距离阈值 |

上述页面在方案讨论时核对。它们没有证明“持续向前流动箭头＋本项目三色＋20/15 km/h 门槛”是通用产品规范；这个组合是针对本项目性能与现有功能的设计。

## 4. 固定几何与安装校准

### 4.1 数据来源与隔离

- 水平基准读取 `CameraCalibration.horizonYNormalized()`，由已保存的俯仰与焦距得到。
- 中心读取 `CameraCalibration.guideCenterXNormalized()`；同一中心用于近端和远端，竖直校准线只决定参考带的视觉中心。
- 继续通过 `AdasCalibrationMode.distanceReady()` 和 `isUsableFor()` 判断标定是否适用，并校验双线结果仍在俯仰 `2°～14°`、中心 `0.5±0.12` 范围内。
- 中心值不作为光学主点、yaw 或车辆横向偏置，不回写前车筛选、距离投影或告警引擎。
- 不新增摄像头序列号要求，不改变 VERSION 2 标定数据格式。

### 4.2 首版模板

几何计算使用原始图像归一化坐标。设保存中心为 `c`，地平线为 `h`：

```text
近端 yN = 0.90
远端 yF = h + 0.06
近端半宽 wN = 0.22
任意行半宽 w(y) = wN × (y - h) / (yN - h)
左边界 xL(y) = c - w(y)
右边界 xR(y) = c + w(y)
```

上述数值是视觉模板初值。`yN` 只是画面下部起点，不表示已检测到真实机盖；`wN` 不是物理车道宽度。远端在地平线下方留白，避免图形挤到远处小目标附近。任一数值非有限、`yF >= yN` 或坐标超出支持范围时隐藏参考带，不继续绘制畸形多边形。

参考带形状只在保存新标定、预览尺寸变化或安全区域变化后重算。车速、前车距离和风险变化只改变显示、颜色及动画，不改变形状。标定尺寸失配时先隐藏，重新校准后才能显示。

### 4.3 坐标与遮挡

- 复用 `CalibrationAlignment.fitCenter()` 的等比例画幅；以根布局避开导航栏和屏幕切口后的内容区为画布。
- 预览、参考带、目标框与校准线共用一套图像到 View 的坐标变换；上下或左右黑边不参与绘制。
- 720p 与 1080p 使用相同归一化模板，不硬编码屏幕像素位置；切换分辨率仍要求对应有效标定，不自动沿用旧尺寸标定。
- 绘制顺序为参考带、箭头、前车框/距离标签、HUD 控件。当前目标框内部和距离标签区域应避让参考带填充及箭头，不能仅靠把文字画在最上层掩盖背景干扰。

## 5. 三色语义与目标有效性

首先检查第 6 节的运行条件和速度开关，然后选择下列视觉状态，对应 `FixedGuideController.Mode`。

| 状态 | 条件 | 参考带 | 箭头 |
| --- | --- | --- | --- |
| 隐藏 | 运行条件不满足、速度开关关闭、目标 `LOST`，或 `TRACKING` 但测距无效 | 不绘制 | 不绘制、停止刷新 |
| 监测中 | 当前分析有效、速度门槛已打开，但没有已确认前车，包括 `NONE` 或 `CANDIDATE` | 绿色渐隐参考带 | 与正常状态相同，3 个箭头循环向前 |
| 正常跟车 | `TRACKING`，观测/测距/目标 ID 一致且有效，没有黄色或红色条件 | 绿色渐隐参考带 | 正常流动 |
| 跟车注意 | 同一有效目标的 `headwayWarning=true`，且没有红色条件 | 黄色渐隐参考带 | 保留弱静态箭头，不继续流动 |
| 前方危险 | 同一有效目标的 `collisionDanger`、`headwayCritical`，或有效保持期内的 `FCW`/`HMW_CRITICAL` 事件 | 红色渐隐参考带，突出目标框警示 | 隐藏 |

“监测中”的绿色仅表示当前监测链路工作，不表示已经确认道路安全。速度达标后，即使暂时没有已确认前车，也显示绿色参考带和 3 个循环箭头；`NONE` 的有效空结果与模型异常、结果过期必须区分，异常不能降级为绿色监测状态。

有效目标要求 `TRACKING`、当前检测框存在、`motion.visible()`、目标 ID 一致、距离有限且大于零，以及结果仍新鲜。`LOST` 期间不延用旧框、旧距离或红色保持；恢复跟踪后使用新的有效观测显示。目标 ID 变化立即清除上一目标的视觉保持和箭头状态。

风险顺序为红 > 黄 > 绿。复用 `AdasDecisionEngine.Decision` 及现有 `displayDecision()` 的目标绑定事件保持，不另算 TTC、THW 或米数阈值。风险标志与已确认音频事件本来就有区别，绘图不得为变色重新调用告警引擎或播放音频。

基线代码会保持告警事件约 1.5 秒，短暂 `LOST` 后跟踪器也可能恢复同一 ID，因此仅检查 ID 不能阻止旧红色恢复。实施时，`LOST`、测距无效或输入失效必须使该目标先前的**视觉事件保持**失效；即使恢复为同一 ID，也只能接收恢复后的当前风险标志和新事件。目标框与参考带共用此失效分界，不能只清新控制器的历史后又直接使用 `displayDecision()` 中的旧保持事件。该处理仅清理展示历史，不重置 `AdasDecisionEngine` 的冷却、LVSA 状态或音频事件。

前车框与参考带必须共用红/黄/绿的风险解析和色值；正式构建仍在测距无效时隐藏目标框，在仅缺少有效速度时保留原有灰框等降级行为。参考带的速度开关不能作用到车辆框。风险升级在下一次 UI 更新及时显示；风险降低仅做约 250 ms 的颜色过渡，目标框与参考带同时使用这份过渡色值，避免短暂异色。沿用有效目标的现有事件保持，不新增另一套长时间告警锁。

## 6. 速度开关、数据时效与生命周期

### 6.1 运行条件

参考带要求页面处于前台、未进入校准页、实时预览可用、模型无错误、当前分析有效、安装配置适用、GPS 速度有效。定位沿用当前精确定位权限与质量校验，不能把无速度当作 0 km/h。

当前代码的时间基准与上限必须保持一致：

| 输入 | 时效要求 | 时间基准 |
| --- | --- | --- |
| 采集/分析结果 | 当前相机会话，年龄不超过 `MAX_OBSERVATION_GAP_NANOS`，当前为 750 ms | `System.nanoTime()`，使用原始帧时间戳 |
| GPS 速度 | 有效定位观测，年龄不超过 2,000 ms，不接受未来时间 | `SystemClock.elapsedRealtimeNanos()`，使用定位测量时间戳 |
| 显示/隐藏持续时间及动画 | 单调计时，退出或会话重置后重新开始 | 为 UI 状态机选用同一单调时钟，禁止用系统日期时间 |

两种输入时间戳不能直接相减。动画 tick 和 UI 心跳不能更新输入时间戳，不能把同一旧分析反复绘制成新观测。仅有实时预览但模型故障时，双线校准仍可用，固定参考带保持隐藏。

### 6.2 速度开关

1. 初始为关闭。在运行条件持续有效时，车速 ≥20 km/h 连续 1,000 ms 后打开；未达到门槛或运行条件失效会清除开启计时。
2. 打开后，车速 ≤15 km/h 连续 1,000 ms 后关闭；重新超过 15 km/h 会清除关闭计时。
3. 在 15～20 km/h 的开区间保持原开关状态，避免阈值附近反复闪烁。
4. 两段有效速度观测之间若出现超出 2,000 ms 的断档，或两帧原始分析时间相隔超过 750 ms，不能把中间未知区间算入连续时间；即使 UI 恰好跨过断档，也要重建显示计时。
5. 数值无效、权限/预览/模型/分析/标定条件失效时，立即关闭并清除门槛计时。恢复后重新满足开启门槛。
6. 单纯目标 `LOST` 或测距无效时隐藏参考带，但在运行条件和 GPS 持续有效的前提下可保留速度开关；恢复有效目标后使用新观测显示，不等待第二轮加速。

“立即隐藏”指下一次 UI 更新/动画 tick 清除，不保留淡出的旧风险图形。静态状态没有逐帧动画时，也必须在输入到期或下一次现有 UI 心跳时清除；绘制过期延迟不应超过当前 250 ms UI 心跳周期。

### 6.3 进入设置、校准与退出

- 紧凑设置弹窗打开时暂停箭头，并使用当前有效状态的静态参考带作为背景；输入失效仍隐藏。
- 进入双线校准页时隐藏参考带，避免与两条可拖动校准线混淆。
- 新标定确认后重建几何并重新满足显示门槛；取消校准不保存新参数，恢复现有配置后重新满足显示条件。
- `onStop`、View 脱离窗口、USB 断开/重连、相机会话重置时停止所有动画回调并清除视觉状态，不能后台继续空转。
- DEBUG 室内模拟允许使用独立、明确标识的合成速度与时间验证门槛；退出模拟必须清空动画和门槛历史，恢复真实数据资格，不保存模拟标定。

## 7. 渐隐和箭头细节

正常跟车的填充不透明度从近端 25% 降到中部约 12%，远端为 0；边缘近端不透明度上限 45%，向远端渐隐。黄色、红色保持同等透明层级，主要通过色相和现有目标警示强调风险，不做全屏闪烁或不透明路面遮罩。

监测中的绿色参考带沿用正常状态的渐隐填充；箭头只使用少量线段/路径，不使用图片序列、粒子或模糊光效。

正常跟车显示 3 个沿中心排列的箭头。以 1.8 秒为周期，三个相位相隔三分之一周期，从近端向远端移动；宽度随所在行的参考带宽度缩小，透明度随远近渐隐。循环重生的近端需做短距离淡入，避免突然跳出。箭头不能移出参考带或进入目标框与距离文字的避让区。

黄色状态将箭头停在固定相位并降低不透明度，不保留持续移动；红色状态隐藏箭头。解除风险、恢复有效目标后从新的动画周期开始，不补播暂停期间的运动。

## 8. 与当前代码的衔接

| 模块 | 基线已有能力 | 实施要求 |
| --- | --- | --- |
| `CameraCalibration` / `CalibrationStore` | 保存和恢复俯仰、焦距、尺寸与 `guideCenterXNormalized` | 只读取已保存配置；不改变投影或持久化格式 |
| `CalibrationAlignment` | 双线边界和 `fitCenter()` 坐标工具 | 复用有效范围与画幅变换，避免再建另一套缩放公式 |
| `MainActivity` | 相机会话、GPS 资格、结果时效、250 ms UI 心跳、告警保持与生命周期 | 提供不可变显示输入及原始时间信息；传递设置/校准/前台状态 |
| `FixedGuideController` | 已接入 | 纯 Java 管理速度开关、目标资格、风险状态及重置；不依赖 Canvas、不计算告警阈值 |
| `FixedGuideGeometry` / `FixedGuideRenderer` | 已接入 | 管理模板几何、缓存路径/渐变及按相位绘制箭头；由 `VehicleOverlayView` 驱动单调时钟与生命周期 |
| `VisualAlertState` | 已接入 | 只保持当前有效目标的视觉事件；失效后增加连续性标记，使 UI 即使错过无效帧也能清旧颜色 |
| `VehicleOverlayView` | 绘制前车框及距离；现有 `selectedColor()` 解析目标颜色 | 提取可共享的风险颜色解析，让参考带与目标框一致；固定绘图独立于关闭的 `drawLane()` 分支 |
| `AdasDecisionEngine` / `AlertAudio` | FCW/HMW/LVSA 风险、事件、冷却和播放 | 本功能不改阈值、事件确认、冷却或播放路径 |

上述职责和数据隔离保持独立，没有为此重构采集、模型或整个 Activity。固定绘图不能通过把 `LDW_ENABLED` 改为 `true` 来开启。

显示输入需要同时携带帧时间戳/尺寸与相机会话、GPS 测量时间戳/车速、跟踪状态/ID、测距有效性、当前风险与视觉事件保持资格，以及标定和前台/设置/校准状态。原始数据、保持事件和动画相位分别管理，不能用“当前 UI 时间＋一个颜色”替代它们。`MainActivity` 的目标框和新控制器必须共同落实第 5 节的视觉保持失效规则。

绘制器最多以 30 FPS 更新箭头相位；它只消费状态，不重新运行推理或运动估计。状态变化可请求一次及时重绘；静态显示无需维持高频 tick。没有动画或颜色过渡时，使用现有状态更新与到期通知即可。

## 9. 实施顺序与交付检查

下列勾选表示代码接入与静态核对完成；用户已确认目标环境编译、权限、室内模拟、设置弹窗和三类声音开关。它们仍不替代道路识别精度、长稳、声压和音频通道验收。

- [x] **状态规则：** 先为第 5、6 节的输入组合、速度门槛和重置写纯 Java 回归用例，再实现控制器；统一红/黄/绿风险解析，车辆框保留原有低速及降级显示。
- [x] **静态几何：** 接入保存的中心和地平线；绘制渐隐参考带；覆盖画幅留边、导航栏安全区、中心边界、720p/1080p 以及目标/文字避让；此阶段先保持箭头静止。
- [x] **动画与生命周期：** 增加正常跟车的箭头循环、黄色静态/红色隐藏、设置暂停、校准隐藏和所有停止/恢复路径；检查输入过期不被动画续期。
- [x] **集成复审：** 沿采集 → 会话/时效 → 标定/速度 → 跟踪/测距 → 风险 → 绘制核对；确认低速 HMW、停车 LVSA、前车框与音频行为保持原规则。
- [x] **验证与交付：** 用户已确认编译、权限、室内三场景模拟、设置弹窗和三类声音开关；本文保留道路识别精度、长稳、声压和音频通道的独立验收边界。

## 10. 验收矩阵

| 场景 | 必须观察到的结果 |
| --- | --- |
| 未校准、旧配置失效或当前分辨率不匹配 | 参考带和箭头隐藏；设置/双线校准入口仍可用 |
| 静止加速：19 → 20 km/h | 未满 1,000 ms 不显示；连续满足后显示；不能按模型帧数近似一秒 |
| 已显示后在 16～19 km/h 波动 | 保持显示，不重复启停 |
| 降至 15 km/h，持续不足/达到 1,000 ms | 未满时保持；达到时隐藏；其间回到 >15 时取消关闭计时 |
| 门槛计时中 GPS/分析帧断档、NaN 或未来时间 | 隐藏并重置，不把未知时间计入连续时间；UI 错过过期窗口也必须识别断档 |
| GPS 跨断档恢复，但新分析尚未发布 | 旧分析只保留灰框/距离，清旧风险和保持；固定参考带等待使用新定位的分析帧，并重新累计开启时间 |
| 无前车但有新鲜有效检测结果且速度达标 | 绿色渐隐参考带和 3 个循环箭头持续显示 |
| 候选确认、正常稳定跟车 | 仍保持绿色渐隐带与 3 个循环箭头；只有风险状态改变颜色和箭头策略 |
| `LOST`、目标框截断或当前测距无效 | 参考带/箭头隐藏，不借用旧距离；恢复后使用新有效观测 |
| HMW 注意 / FCW 或极近风险 | 黄 / 红与目标框一致；黄色箭头静止减弱，红色无箭头；音频由原事件路径触发 |
| 风险条件解除、目标 ID 更换 | 仅同一有效目标可使用已有事件保持；换目标不继承旧红色或动画相位 |
| 红色事件 → 短暂 `LOST` → 同 ID 安全恢复，仍处于原 1.5 秒保持期内 | 目标框与参考带均不恢复失效前的红色事件；决策冷却/音频不被重置；恢复后新风险仍可正常显示 |
| 低于参考带显示速度时出现极近 HMW | 参考带隐藏，原有目标框与符合条件的 HMW 事件仍正常工作 |
| 停车后前车起步 | 参考带因低速门槛隐藏，原有 LVSA 条件满足时仍提醒 |
| 预览正常但模型故障，或结果年龄 >750 ms | 固定参考带隐藏；不能显示绿色监测状态；校准仍依赖独立预览快照 |
| 模型停止输出但动画原来仍在循环 | 原结果自然到期，动画停止；不因持续重绘延长有效期 |
| 中心 0.38 / 0.62，俯仰 2° / 14°，不同 HFOV | 模板保持图像坐标，正常收拢；不反向修改测距或筛选中心 |
| 720p/1080p、4:3 回退画面、上下/左右黑边、底部/侧边导航栏 | 标定尺寸门控正确；预览与图形无错位、拉伸或侵入系统栏 |
| 连续目标左右跳动、弯道画面 | 图形位置仍固定；风险变化只换色，不将固定图形伪装为实时路面边界 |
| 打开设置、进入/取消/保存校准 | 设置中暂停动画；校准中隐藏；取消不保存；确认后重建几何 |
| USB 重连、后台/前台、View 销毁重建、模拟退出 | 回调和状态完整停止/重置；新会话不继承旧图形或资格 |

纯 Java 用例重点覆盖时间边界、输入有效性、风险优先级、目标切换、模板计算和中心参数隔离。实际透明度、触摸、导航栏适配、绘制性能、音频与道路效果需要设备验证，不能由纯逻辑测试替代。

## 11. 性能与验证口径

- 不新增模型、采集流、NV21 拷贝、位图合成或推理线程；模型仍按现有约 5 FPS 上限分析。
- 复用 Paint、Path、渐变和固定数量箭头数据。几何在标定/布局变化时重算，禁止在每个动画帧创建整屏 Bitmap 或大量临时对象。
- 动画隐藏、静态监测、设置暂停或页面退出后不保留高频自刷新。颜色过渡结束后也停止其动画回调。
- 在同一台 4450、同一摄像头/分辨率/场景下分别记录关闭参考带、静态参考带、流动箭头三组数据；采集 CPU、PSS、帧耗时、结果年龄、GC 与温升，注明统计口径。
- 分别验证 720p 与实际协商成功的 1080p；优先保证告警链路与预览响应，30 FPS 是动效上限而非必须牺牲主链路达到的指标。若出现明显额外负担，先降低动画帧率或减少箭头，不改变告警门槛补偿。
- 设备端连续运行至少 30 分钟，并重复设置/校准进入退出和 USB 重连，检查动画回调、内存及资源是否持续累积。

现阶段没有该功能的设备测量数据，不承诺固定 CPU 增量、内存增量或“4450 已验证流畅”。

## 12. 当前实现状态与文档维护

| 条目 | 2026-09-21 当前代码状态 |
| --- | --- |
| 双线校准与视觉中心持久化 | 已实现并供固定参考带读取 |
| 前车检测、跟踪、测距、FCW/HMW/LVSA | 已接入；实车精度与效果仍需验证 |
| 固定参考带、三色共享解析、速度开关、3 箭头循环 | 已接入并按用户目标环境验证显示链路 |
| 控制器、固定几何、视觉保持及 LVSA 恢复回归用例 | 已补充；本轮不重复执行 Gradle/JUnit |
| 三类声音开关、音频优先级和设置弹窗滚动 | 已接入；用户已确认即时生效 |
| 本文设备验收场景和性能对照 | 保留长稳、声压、测距精度和道路误报漏报记录 |

本文是固定参考带功能的设计依据，与现有 [V1 实施计划](HCT_ADAS_Final_Solution_and_Implementation_Plan.md) 和 [标定与输出规格](HCT_ADAS_Final_Design_Lane_Calibration_and_Outputs.md) 配合使用。编译、权限、模拟、设置弹窗和声音开关的用户验证已记录；长稳、声压、测距精度及道路误报漏报仍按设备/实车验收边界记录。

### 本次审查与修正记录（2026-09-20）

- 覆盖 USB 会话/队列、推理、跟踪、测距、GPS 时效、安装校准、FCW/HMW/LVSA、视觉显示、音频优先级和退出/重连路径；采用自审及交叉独立静态复核。
- 分析帧与 GPS 原始观测发生断档时，即使 UI 没看到中间的过期状态，也要重新累计开启条件；两个时钟分别检查，不互相相减。
- 目标失效或 GPS 无效在生产者侧清理旧视觉事件，防止 UI 跳帧后同 ID 恢复旧红色；新 GPS 先于新分析到达时，显示快照也校验原始定位的连续性，避免旧分析恢复风险资格。不重置真正的告警冷却。
- 修正原有 LVSA 恢复观测跨越 600 ms 失效容忍期时沿用旧等车状态的问题，并补充边界用例。
- 调试扬声器测试复用真实告警的优先级入口，不能降低正在播放的 FCW 优先级；播放失败不占用优先级。按包内 WAV 文件头核对播放时长，优先级保持至少覆盖各音频长度，避免 HMW 等样本尾段提前失去保护。
- 显示只在短同步区读取一致快照，日志、音频服务查询和 View 更新在锁外；动画不重复格式化目标文字、不重新推理。
- 用户已提供 `:app:compileDebugJavaWithJavac --no-daemon` 成功记录，并确认权限、室内模拟、设置弹窗和三类声音开关正常。本轮只刷新文档，未重复编译或执行 JUnit；静态审查和室内模拟仍不能替代车机长稳、声压、测距精度及道路识别验收。

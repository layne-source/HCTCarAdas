# HCT ADAS V1 精简版方案与实施计划

> **文档状态：** V1 精简版实施与验证基线；V2 双模型模块已从仓库移除
>
> **适用平台：** Qualcomm SM4450 / Android 16
>
> **交付形态：** 当前目录内独立 HCTADAS Java/Gradle 工程，复用车机现有 UVC 摄像头底层能力
>
> **最低运行环境：** Android 13（API 33）

> **当前落地边界（2026-09-19）：** 先交付 V1 精简前车版。生产构建隐藏 FPS、队列丢弃、推理耗时、候选框和车道/标定参考线；正常驾驶页显示时间、车速和点击后短暂出现的无背景设置入口，前车距离跟随真实跟踪目标框显示，FCW/HMW/LVSA 以目标标签和报警音表达；不显示中央安装提示。`LDW_ENABLED=false`，不再让行驶中车道自动学习阻塞前车功能。安装配置只选择车辆类型和 HFOV，再通过水平线/竖直线双线校准俯仰与安装中心；按当前分辨率保存，不绑定摄像头序列号，保存后状态为 `DISTANCE_READY`。车道检测类暂保留，便于后续恢复，不属于本版功能验收范围。
>
> 下文涉及 LDW、车道几何和自动学习的段落保留为后续恢复设计；第 3 点描述当前双线安装校准。

## 项目级复核结论

当前工程已形成可运行的前台原型链路：USB/UVC 采集、NV21 快照、有界队列、LiteRT 检测、前车跟踪、单目测距/运动量、FCW/HMW/LVSA 决策、UI 展示和固定音频均已接入；车道检测类仍保留但本版不启用。本轮工作区还加入了 `LOST` 重获速度历史清理、LiteRT 运行时退避、精确定位权限门控、USB 生命周期/格式协商和音频高优先级抢占。用户已确认此前版本编译正常，但尚未完成本轮目标设备和实车验证；本轮也没有重新编译或运行 Gradle。

量产规划必须以以下事实为准：

- 当前 V1 双线安装校准保存后进入 `DISTANCE_READY`，使用配置的高度、HFOV 和俯仰角开放距离能力；历史 `CALIBRATED` 只代表旧车道 pitch 学习收敛，不能等同于完整相机内参、畸变、yaw 或横向安装偏差校正。摄像头重新打开后读取配置，并在使用时校验当前画面分辨率，不要求序列号；改变安装位置、摄像头或分辨率后应重新校准。
- HMW 已加入 THW，并保留固定距离兜底：速度 ≥15 km/h 时普通提醒为 `THW≤1.2 s 或距离≤8 m`；极近规则为 `THW≤0.6 s 或距离≤4 m`；1～15 km/h 按距离兜底，停车时抑制极近音频但保留 ≤4 m 的普通视觉提示。
- FCW 由连续 3 个分析帧和 `FCW_CONFIRM_MILLIS=200` 的时间窗共同确认；实际触发延迟仍随采样间隔、丢帧和设备性能变化，不能只用帧数推断。
- LDW 代码仍是固定 ROI 的亮边缘原型，但当前生产开关关闭，真实帧不会执行车道检测、车道绘制或 LDW 决策；模拟车道场景也不在本版验收范围。
- 报警音仍是 `SoundPool(USAGE_MEDIA)` 加 `ToneGenerator(STREAM_MUSIC)` 的软件路径；高优先级事件现在会停止已播放的低优先级 SoundPool 流和 ToneGenerator，但没有独立 ADAS 音频通道、AudioFocus、硬件蜂鸣器或正式构建开关。
- Activity 离开页面就停止采集和消费，当前没有前台服务、开机启动或后台持续监测能力。

## 1. 最终结论

项目具备实现条件。车机已经存在可运行的 USB 摄像头方案：

- `hct/apps/USBCamera/HCTUSBCamera` 已完成 UVC 设备枚举、权限申请、预览和录制。
- `HCTUSBCamera/MainActivity.java` 已配置 MJPEG、1280×720，并通过 `onPreviewResult(byte[] nv21Yuv)` 输出 NV21 帧。
- `hct/apps/USBCamera/HCTUSBCamera/libs/libusbcamera.aar` 已包含 Java UVC 封装。
- `hct/device/libusbcamera` 已提供 `libUVCCamera.so`、`libuvc.so`、`libusb100.so` 等设备侧库。
- `hct/device/qssi/qssi.mk` 已将应用和 `libUVCCamera` 纳入目标产品。

因此，ADAS 不需要重新实现 USB 驱动或 UVC 协议，重点转为独立的帧分析、算法和报警状态机。

文档中的 CPU、RSS、误报率和实际报警效果仍需在目标设备与实际摄像头上验证，不能在验证前视为承诺指标。

### 1.1 当前进度与实测基线

- 独立 Java 工程、USB 权限/热插拔、NV21 队列、消费线程、车辆检测和检测框显示已实现；用户确认上一版编译及设备运行正常。
- 当前推理使用 LiteRT 1.4.1 Java 接口和内置 EfficientDet-Lite0 UINT8 模型，输入为 320×320。原 JNI/NCNN 设计为早期候选方案，不是当前实施路线。
- 本轮新增前方候选选择、连续跟踪、目标 ID、漏检与换车处理，以及搜索区域和跟踪状态显示。第 3 点同时新增标定参数模型、校验、SharedPreferences 保存/读取、点击配置对话框和主点辅助线；用户已确认编译正常，但本轮没有重跑编译、单元测试或设备验证。
- 已接入安装配置距离、同 ID 距离/逼近速度/TTC 观测、GPS 速度读取、FCW/HMW/LVSA 状态计算和固定短音频输出；LDW 代码仅保留为关闭的后续能力，这些报警阈值仍未完成实车验证。
- 当前未实现后台前台服务、开机启动及 TTS；GPS 权限未授予或速度过期时，FCW/LVSA 依赖速度的条件自动静默，HMW 仍按有效距离工作。

以下均为用户提供的上一版实测，不能视为本轮跟踪功能的验证结果：

| 指标 | 观测值 | 口径与限制 |
|---|---|---|
| 采集帧率 | 最高 31 FPS | NV21 回调计数；与模型分析帧率不同，实测约 84% 的采集帧不执行推理 |
| 分析采样 | 当前代码间隔 200 ms，上限约 5 FPS | 回调间隔不整除时实际可能低于 5 FPS；没有改为 10 FPS（90 ms 推理在 100 ms 间隔下约占满消费线程） |
| 单帧推理 | 60–90 ms | 现有计时只含预处理、模型调用和检测后处理，**不含车道检测、跟踪、测距与决策**，也不含完整采集/排队/显示延迟 |
| 进程 CPU | top 显示 89.6% | 800% 总标尺下约为整机 11.2% 的 CPU 时间占比，不代表不同核心的等效算力 |
| RSS | 255754 KB，约 249.8 MiB | 包含完整共享驻留页面 |
| PSS | 114476 KB，约 111.8 MiB | 按比例分摊共享页面，与 RSS 不可替换 |
| Native / Java / Graphics | PSS 约 56.3 / 12.7 / 16.9 MiB | App Summary 分类，无法据此精确分配到 UVC 或模型 |
| 内存变化 | 两次相隔约 13 分 21 秒，PSS 增加 256 KB | 两个采样点接近，尚不能证明长期无泄漏 |

原 CPU ≤5%、RSS ≤80 MB 是待验证目标，当前数据未达到这两个数值；不能将目标改称 PSS，也不能据此认定出现运行故障。目标是否调整需明确确认，本轮不调整采集参数、推理参数或性能验收标准。

## 2. 设计边界

### 2.1 必须实现

1. USB 摄像头持续采集和断连重连。
2. NV21 帧进入独立分析队列，不阻塞 UVC 回调线程。
3. 车辆检测、单目距离估计和 TTC 计算。
4. FCW、HMW、LDW、LVSA 四类功能的状态机。
5. 固定报警音作为主报警路径；TTS 仍是未实现的后续能力。
6. 摄像头参数标定和运行状态显示。
7. CPU、内存、帧率、延迟、温升和断流监控。

### 2.2 明确不做

- 不修改 AOSP、Vendor、HAL、SELinux 或 init。
- 不接入 CAN/LIN、毫米波雷达和超声波传感器。
- 不把 ADAS 逻辑直接耦合到录制 UI 和录像文件流程。
- 不宣称自动驾驶、制动控制或安全等级认证。

## 3. 最终架构

```text
USB 摄像头
    ↓
现有 libusbcamera / libUVCCamera
    ↓ NV21 预览帧
UsbCameraSource（采样及 NV21 快照）
    ↓
FrameDispatcher（容量 2，满载丢旧帧）
    ↓ 独立 FrameConsumer 线程
Nv21Preprocessor → LiteRtVehicleDetector
    ↓ 车辆检测框
LeadVehicleTracker（前方候选选择、几何匹配、ID 与状态）
    ↓ 检测与跟踪合并快照
MainActivity / VehicleOverlayView

标定 → 测距 → 距离/相对速度平滑 → TTC
                        ↓ 加入有效 GPS 速度
                FCW / HMW / LVSA 决策接入
车道线轻量检测 → LDW；固定音频事件输出
```

### 3.1 应用层模块

当前已在本目录建立独立 `HCTADAS` 应用工程，使用 Java，`minSdk=33`，`targetSdk=36`，并复制现有 `libusbcamera.aar` 到 `app/libs/`。后续算法和帧分析代码继续放在该工程内，不修改录制应用源码。

```text
app/libs/libusbcamera.aar
app/src/main/assets/models/                 # 模型、标签、许可证
app/src/main/java/com/hct/adas/
├── UsbCameraSource.java                    # 权限、UVC 帧回调与生命周期
├── FrameDispatcher.java                    # 有界队列
├── FrameConsumer.java                      # 推理线程、错误统计
├── Nv21Preprocessor.java                   # NV21 → RGB、等比例缩放与补边
├── VehicleDetector.java                    # 检测接口与不可变结果
├── LiteRtVehicleDetector.java              # 模型加载及车辆类别过滤
├── LeadVehicleTracker.java                 # 本轮：前方候选与连续跟踪
├── CameraCalibration.java                  # 几何参数、标定状态与单目投影模型
├── CalibrationStore.java                   # 状态、进度与版本化本地持久化
├── AutoCalibrationLearner.java             # 动态车道灭点提取、收敛判定与慢速跟踪
├── LaneDepartureDetector.java              # NV21 Y 分量轻量车道边缘与偏离
├── AdasDecisionEngine.java                 # 独立规则与冷却决策状态机
├── MainActivity.java                       # 前台生命周期、紧凑设置/双线校准与状态显示
└── VehicleOverlayView.java                 # 检测框、车道、主点/灭点辅助线与向导指示
app/src/test/java/com/hct/adas/              # JVM 单元测试用例
```

### 3.2 帧处理约束

- UVC 回调只负责接收和投递，不执行模型推理。
- 使用容量为 2 的有界队列；队列满时丢弃旧帧。
- 预览按候选顺序协商：MJPEG 1280×720、640×480、1920×1080，再尝试 YUYV 1280×720、640×480、320×240；模型固定输入 320×320，保持比例并补边。真实 AAR/摄像头是否接受每个候选仍需设备验证。
- UVC 回调每隔至少 200 ms 复制一个 NV21 帧入队，原生回调缓冲返回后会复用，因此当前不是零拷贝；720p 单个快照为 1382400 字节。
- 独立消费线程串行执行检测和跟踪，LiteRT 推理线程数为 1；复用模型 RGB 输入和张量输出缓冲。
- 跟踪只处理少量检测框，不增加图像副本或新模型。检测框和跟踪状态以同一份不可变快照发布给 UI。
- 拔插、错误、页面停止/重启时清除显示并隔离旧会话；跟踪器由消费线程重置。画面宽高变化也重新建立目标。SurfaceTexture 由监听器在相机线程清理后单次释放并返回 `false`，避免框架重复释放。
- 超过 750 ms 的分析结果不进入跟踪；UI 在刷新时隐藏超过该时限的结果。此时间从 NV21 回调时间戳计算，不是传感器曝光时间。

## 4. 功能实现顺序：按确认的 7 点分阶段推进

采集及车辆检测已经形成上一版可运行基线。后续仍按以下顺序推进，每轮控制改动范围，保留用户编译与设备验证环节。

### 第 1 点：前方车辆候选选择（本轮代码已写入，待验证）

- 从 car/bus/truck 检测框中筛选置信度不低于 0.5、坐标有效、底边中心落入前方搜索区的候选。
- 有效标定且尺寸匹配时，搜索区上边采用标定地平线；无有效标定时回退 y=0.45。上边左右 x=0.40/0.60，下边 y=1.0、左右 x=0.20/0.80，中间按深度插值。竖直参考线不改变该搜索区的横向中心。
- 优先选择底边较低且靠近中部的框。底边位置只用于候选排序，不代表已经计算了真实距离。
- 搜索梯形仅用于内部候选筛选，驾驶页不绘制。弯道、相机偏置及邻车遮挡可能选错候选；本版不声称可靠识别“本车道前车”。

### 第 2 点：连续跟踪（本轮代码已写入，待验证）

- 用框重叠率（IoU）、中心位移和面积比例匹配前一目标；车辆类别在 car/bus/truck 间跳动不单独触发换 ID。
- 候选至少连续出现 3 次、覆盖至少 400 ms 才获得确认 ID；漏检或不匹配会重新确认。
- 短暂漏检保留 ID，但输出 LOST、检测框为空。超过 750 ms 未匹配则结束旧目标。
- 原目标仍可见时，另一个底边明显更低的候选必须持续满足确认条件才可替换；确认换车使用新 ID。
- 两个不同框的匹配分数接近时暂不认定旧目标。重复或倒序时间戳不推进跟踪状态。
- 纯几何关联不能保证真实车辆身份，遮挡交叉时仍可能误关联；确认目标还需有效测距及对应速度条件才能触发报警。
- UI：正式构建仅显示具有有效测距的当前目标框及距离标签；灰色表示缺少测距或速度条件，绿色正常，黄色注意，红色危险。候选框仅在 DEBUG 构建显示。

本轮验证场景：

1. 单车进入搜索区后确认，正常小幅抖动与类别变化保持 ID。
2. 多车检测顺序改变、侧方大车出现，目标不因数组顺序或框面积直接跳变。
3. 暂时漏检显示“暂时丢失”，隐藏旧黄框；短时恢复保留 ID，超时恢复获得新 ID。
4. 前车仍在、较近车辆持续切入时确认换 ID；单帧干扰不换车。
5. USB 拔插、退出重进、流中断和分辨率变化后，不显示或继承旧会话目标。
6. 对比原 31 FPS 采集、60–90 ms 推理基线，记录新版本 CPU、PSS/RSS 与跟踪状态；心跳日志中的 `Age` 字段用于核对端到端结果年龄。
7. 新增 JVM 用例覆盖以上几何与时序边界；本轮只做静态检查，测试执行及实机结果待用户验证。

### 第 3 点：摄像头安装配置与双线校准（当前实现）

当前 V1 不再依赖行车自动车道标定，也不要求车主拿已知距离目标做测量。设置入口是驾驶页右下角的无背景按钮，点击屏幕后短暂显示，约 2.5 秒无操作自动隐藏。设置弹窗保持居中紧凑，只保留车型、镜头 HFOV 和 ADAS 校准三个入口。

#### 1. 配置输入

- 车型使用轿车、SUV、货车三个高度预设，作为单目距离模型的相机高度输入。
- HFOV 使用 90°、100°、120° 三档，按当前画面宽高换算归一化焦距。
- 不读取、不保存摄像头序列号；配置只与当前画面分辨率绑定。更换安装位置、摄像头或分辨率后重新校准。

#### 2. 双线校准流程

点击“ADAS 校准”后关闭设置弹窗，进入全屏相机画面：

1. 拖动水平线对齐道路地平线，用于反算俯仰角；
2. 拖动竖直线对齐车头中心，保存为后续固定车道参考图形的中心；当前不绘制该固定图形，也不将此线用于 yaw、光学主点或前车归属校正；
3. `CalibrationAlignment` 将俯仰限制在 `2°~14°`、中心线限制在 `±0.12`；任一越界显示红线并禁用“校正”；
4. 两条线有效后保存 `CameraCalibration`，状态直接进入 `DISTANCE_READY`，前车距离、FCW、HMW、LVSA 可继续使用；取消则不改动已有配置。

校准页只提供必要的两条线和“取消/校正”操作，不绘制车道线、候选梯形或额外诊断面板。校准使用有效 UVC 帧的尺寸、时间戳和会话编号，独立于模型结果；黑边不参与坐标换算。导航栏和屏幕切口由根布局统一避让，预览、两条线和目标框共用同一显示区域。驾驶页不显示中央安装提示；未配置时仍可通过短暂出现的设置按钮进入配置。

#### 3. 失效与降级

- 旧 VERSION 1 配置可能直接保存了默认俯仰角，升级到 VERSION 2 后要求重新确认双线。当前配置由 `CalibrationStore` 读取，再由 `distanceReady/isUsableFor` 检查帧尺寸；尺寸不匹配时禁用测距，需要重新校准。
- 目标丢失、几何无效或分析结果过期时不显示距离或触发相关告警，不把 `NaN` 当作 0；仅车速失效时保留有效距离和距离型 HMW，暂停依赖速度的 FCW/LVSA。
- `LDW_ENABLED=false` 时不运行车道检测、自动 pitch 学习和车道绘制；`LaneDepartureDetector`、`AutoCalibrationLearner` 仅保留作后续恢复参考。

历史的“安装向导＋行驶自学习”方案仍保留在旧审查记录中，但不属于当前 V1 的运行链路或验收条件。

### 第 4 点：单目距离估算（代码已接入，实测待做）

- 基于已确认目标的框底边中心和相机参数进行地面投影。
- 仅在平面道路、接地点可见、参数有效且结果在可信范围内时输出估算距离。
- 目标丢失、框底边被截断、接近地平线、坡道或相机姿态异常时标记无效。
- 以已知距离的静态车辆验证误差，再进入行驶场景。

### 第 5 点：距离平滑、相对速度与 TTC（代码已接入，实测待做）

- 使用真实帧时间间隔平滑同一 ID 的距离并估计相对逼近速度。
- 仅对有效逼近状态计算 TTC；目标切换、失效或时间间断时重置历史。
- 明确滤波启动期及可信度，避免距离跳变被解释为高速逼近。
- 同步评估端到端延迟；单帧检测的 60–90 ms 不是完整报警延迟。

### 第 6 点：接入 FCW/HMW/LVSA 并显示状态（代码已接入，实测待做）

- 将有效距离、相对速度、目标可见性和面积接入现有规则类。
- 补齐 GPS 权限、速度时间戳与有效性判断；速度缺失不能当作自车静止。
- 接入前复核当前规则中的连续帧触发、目标切换、异常数值和冷却逻辑，使其适应实际抽样间隔与短暂漏检。
- UI 显示估算距离、TTC、条件不足及预警状态；当前正式构建在事件产生时就会请求播放报警音，尚无独立验收开关。
- 验证静止目标、跟车、前车起步、目标切入/丢失和 GPS 失效场景。

### 第 7 点：LDW 与声音报警（原型已接入，实车验证和后台能力待做）

- 已加入基于 NV21 Y 分量的轻量边缘搜索、连续置信度、偏离持续时间和速度门限原型；需用真实道路帧验证，不能视为完整车道线算法。
- 车道置信度、有效速度、偏离持续时间及冷却共同决定 LDW；没有转向灯信息时不能保证区分主动变道。
- 车道线缺失、GPS 无效等状态下静默相关报警；音频实际采用 `SoundPool` 媒体通道和 `ToneGenerator(STREAM_MUSIC)` 软件回退，不能称为硬件蜂鸣保底，也不能绕过媒体静音。室内模拟提供扬声器请求播放测试，但不能代替实车声压和音频焦点验证。
- 白天/夜间、逆光、雨雾、弯坡道、隧道、震动和相机偏移均需实测。
- 完成音频焦点、后台运行、开机启动和权限策略验证，再做长时间资源、温升及故障恢复收口。

### 4.8 场景判定矩阵（当前实现基线）

| 场景 | 必要输入与门控 | 当前判定 | 丢失/异常行为 | UI/音频结果 |
|---|---|---|---|---|
| FCW | 已确认同 ID 目标可见、有效安装配置（当前保存为 `DISTANCE_READY`）且尺寸匹配、GPS 速度有效 | 速度 ≥20 km/h；距离 >0；逼近速度 >0；TTC ≤2.4 s；连续 3 个分析帧且跨度 ≥200 ms | 目标、速度、距离或逼近速度无效会清零连续计数；大时间间隔重置分析 | 红色危险状态，`FCW` 音频事件，3 s 冷却 |
| HMW 普通 | 目标可见且距离有效 | 速度 ≥15 km/h 时 `THW≤1.2 s 或距离≤8 m`；停车停稳（≤1 km/h）时保持静默（除非贴死 ≤4 m）；低速段按距离≤8 m | 目标/距离无效时静默；停车时主动抑制避免视觉骚扰 | 黄色/注意状态；无独立音频事件和冷却 |
| HMW 极近 | 目标可见、距离有效；逼近速度未显示拉开（NaN 或 ≥−0.5 m/s） | 自车速度 ≤1 km/h 静默；速度 ≥15 km/h 时 `THW≤0.6 s 或距离≤4 m`；更低速按距离≤4 m | 自车静止、目标/距离无效时不发事件 | 红色危险状态，`HMW_CRITICAL` 音频事件，4 s 冷却 |
| LVSA | 同 ID 目标可见、GPS 速度有效且 ≤1 km/h、距离/面积/逼近速度有效 | 目标在 3–9 m 且稳定 3 s；随后距离增加 ≥2.5 m 或面积下降 ≥15%，连续 2 个分析帧 | 容许 600 ms 短暂目标遮挡；目标切换会清空决策状态 | `LVSA` 音频事件，10 s 冷却 |
| LDW（关闭的保留能力） | 当前 `LDW_ENABLED=false`，不参与驾驶决策 | 保留规则：速度 ≥50 km/h、置信度 ≥0.35、偏移绝对值 ≥0.12，持续 1 s | 当前不运行检测或学习 | 本版无 LDW 绘图或音频事件 |

连续帧规则与时间窗（3 帧 + 200 ms）协同生效。目标事件保持已绑定 `heldAlertTargetId` 并在目标切换/丢失时清空；音频优先级锁支持高优先级主动抢占并关停低优先级流。
## 5. 验收指标

### 5.1 必须达成

- USB 摄像头正常识别、采集和断连恢复。
- 分析链路不阻塞预览和系统主线程。
- FCW/HMW/LVSA 状态机可复现并可记录；本版不验收 LDW。报警总开关和量产验收开关尚未实现。
- 发生输入丢失或几何无效时进入安全静默；GPS 不可用只静默依赖速度的 FCW/LVSA/LDW，HMW 普通提醒仍以有效距离为准；车道线不可见时静默 LDW。
- 运行 30 分钟以上无崩溃、无持续性内存增长、无明显系统卡顿。

### 5.2 需要实测后确定

- CPU 是否能稳定控制在原目标 5% 以内；先约定是单核标尺还是整机归一化标尺，并记录采样时长。
- RSS 是否能控制在原目标 80 MB 以内；同时记录 PSS，不能用 PSS 达标替代 RSS 达标。
- 实际推理延迟和端到端报警延迟。
- 各道路场景下的误报率和漏报率。
- 夜间、雨雾和坡道场景的有效性。

### 5.3 当前证据与未覆盖项

- 当前工作区包含 15 个 JVM 测试类、147 个 `@Test` 方法，覆盖跟踪、几何、决策、校准守卫、模拟场景、队列、USB 契约和速度历史清理等纯 Java 逻辑；本轮未执行 Gradle 或全量 suite。
- 历史临时 `javac + JUnit` harness 曾执行 106 个纯 Java 用例并通过，其中包含几何和车道用例；USB 契约测试仍需 Android classpath，未在该 harness 重跑。这不等同于 Android/Gradle 全量通过，本轮也未重新执行。
- 没有 Android instrumented 测试，尚未覆盖真实 NV21 车道提取、UVC 权限/热插拔/Surface 生命周期、LiteRT 真模型端到端、音频设备出声和 Activity 重建。
- 双线角度、中心范围、画面留边和确认结果由 `CalibrationAlignmentTest` 覆盖；实际触摸、导航栏适配、返回/后台取消和相机重连仍需设备验证。
- `AdasSimulator` 的 LDW 场景直接注入 `available=true` 的车道 Observation，不能证明真实图像算法在白天/夜间、逆光、雨雾、弯坡和震动下可用。
- 用户已确认上一版编译正常；本轮进行全链路源码审查、独立复审、Java 语法/XML/资源引用与 diff 静态检查，未编译或运行测试；设备和实车效果仍由用户在目标环境验证。

## 6. 主要风险和处理原则

| 风险 | 处理原则 |
|---|---|
| USB 权限无法自动恢复 | 根据目标产品验证预装/签名和系统授权行为；不能靠重复请求权限解决。 |
| UVC 回调阻塞 | 回调只入队，算法独立线程消费，队列满时丢帧。 |
| NV21 每帧产生 Java 分配 | 优先确认现有 AAR 行为；必要时改为 Native 缓冲或降低分析频率。 |
| 单目测距受俯仰角影响 | 标定必选；坡道和震动同样影响距离差分与 TTC，异常数据必须失效，不能只靠滤波补救。 |
| 几何跟踪误关联 | 采用确认窗口、歧义拒绝和切换新 ID；仍需实车验证，当前搜索区不代表本车道。 |
| GPS 在隧道不可用 | 进入降级状态；没有可靠车速时不强行触发依赖速度的报警。 |
| GPS 质量未仲裁 | 当前仍有水平精度 ≤50 m、速度 ≤180 km/h 和 >15 m/s² 加速度突变过滤，但没有 provider 优先级、位置跳变仲裁；车速链路要求 FINE，只有 COARSE 时保持失效并提示升级权限。 |
| 标定继承错误 | 配置读取后按实时分辨率门控，不读取 USB serial；更换安装位置、摄像头或分辨率后由用户重新执行双线校准。同分辨率换摄像头无法自动识别。 |
| 标定状态含义过宽 | `CALIBRATED` 只代表 pitch 学习收敛，不能代表完整内参、畸变、yaw 或横向安装偏差已校正。 |
| 相对速度异常观测 | 首差分为临时速度；相邻原始差分同向且符合 12 m/s² 变化约束后才初始化限幅滤波，不一致则归零等待，避免启动回摆被锁成持续逼近。`LOST`、ID/时间/标定变化清空历史，真实噪声与急刹响应仍需实测。 |
| 帧数触发延迟漂移 | FCW 仍连续 3 帧、LVSA 连续 2 帧；`FCW_CONFIRM_MILLIS=200` 已参与判定，量产需基于 capture timestamp 的时间窗口和端到端延迟上限。 |
| USB 原始控制块释放 | 静态核验 AAR 后显式保存原始控制块；关闭顺序为 camera、原始控制块、monitor。SurfaceTexture listener 手动释放后返回 `false`。长时间热插拔及 FD 数仍需设备验证。 |
| 推理运行时故障 | 初始化失败保留 5 s 重试；运行时异常按 5/10/20/40 s 有界指数退避，消费线程停止前释放 detector。仍需真模型/设备故障注入验证 UI 告警与最终熔断策略。 |
| 打开阶段 watchdog 覆盖范围 | 已有 8 s opening watchdog；视频流确认后另有 3 s 无帧 watchdog，自动重试最多 3 次并保留手动重试入口。native 调用自身若阻塞，仍受 AAR 行为限制。 |
| 报警竞争与目标切换 | UI 事件保持已绑定目标 ID 并在切换/丢失时清空；`AlertAudio` 高优先级事件会主动停止低优先级 SoundPool 流和 ToneGenerator。AudioFocus、独立通道和实车声压仍未验证。 |
| 录制应用回归 | HCTADAS 独立复用库，不修改 HCTUSBCamera 业务逻辑。 |
| TTS 延迟或抢占音频 | 固定报警音作为主报警路径，TTS 只做辅助播报。 |

## 7. 预计周期

| 版本 | 周期 | 交付内容 |
|---|---:|---|
| 采集与性能原型 | 3~5 个工作日 | USB/NV21/LiteRT 检测基线，上一版已有设备数据 |
| 功能原型 | 5~8 个工作日 | FCW、HMW、LDW、LVSA 基本状态机 |
| 实车稳定版 | 5~10 个工作日 | 标定、降级、报警收口和实车验证 |

以上为初期估算，不是当前剩余工期或交付承诺；实际周期取决于标定输入、目标设备和实车测试条件。

## 8. 实施入口

后续实施约定：

1. 在当前 `HCTCarAdas` 独立 Git 中继续开发，不修改 `HCTUSBCamera`。
2. 使用现有 Java/LiteRT 链路按第 4 节的 7 点顺序推进；当前代码已接入第 1–7 点的原型能力，但场景阈值、故障恢复和 UI/音频量产收口仍待验证与补强。
3. 用户编译及验证本轮目标框/ID、标定对话框、距离/TTC/预警状态和固定音频后，进入实车阈值标定。
4. 未获得标定和有效速度前，不把候选目标输出当作可用预警结果。
5. 每轮标注代码状态、静态检查、编译/测试及实车验证结果；不把上一版数据当作本轮验证。
6. 遵循项目约定：用户负责编译；本轮未主动构建、打包、运行项目；文档复核完成后单独提交，便于回退。
